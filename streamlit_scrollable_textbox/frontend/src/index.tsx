import React from "react"
import ReactDOM from "react-dom"
import ScrollableText from "./streamlit_scrollable_textbox"

ReactDOM.render(
  <React.StrictMode>
    <ScrollableText />
  </React.StrictMode>,
  document.getElementById("root")
)
